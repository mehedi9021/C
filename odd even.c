#include<stdio.h>
int main()
{
    int n, mod;
    scanf("%d",&n);
    mod = n%2;
    if (mod==0);
    {
        printf("Even", &mod);
    }
    else
    {
        printf("Even",&mod);
    }
    return 0;
}
